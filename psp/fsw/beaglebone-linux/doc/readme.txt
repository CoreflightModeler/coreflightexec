This package provides all the platform support needed to build a cFS system on the
Beaglebone running under linux.  The package uses the basic linux timer and memory 
support.  Upgrading the package to use the beagelbone hardware timers will improve 
performance.